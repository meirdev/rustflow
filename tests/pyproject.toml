[project]
name = "tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.ruff.lint]
extend-select = ["I"]

[dependency-groups]
dev = [
    "protobuf>=7.36.2",
    "pyarrow>=25.0.1",
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
