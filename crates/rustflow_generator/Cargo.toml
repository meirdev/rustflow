[package]
name = "rustflow_generator"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.32", features = ["derive"] }
ipnet = "2.11.0"
rustflow_parser = { path = "../rustflow_parser" }
